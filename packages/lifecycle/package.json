{
  "name": "@acl/lifecycle",
  "version": "0.1.0",
  "private": true,
  "description": "ACL 生命周期处理引擎（subject_removed 默认语义）",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@acl/shared-types": "workspace:*"
  }
}
