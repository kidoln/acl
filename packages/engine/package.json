{
  "name": "@acl/engine",
  "version": "0.1.0",
  "private": true,
  "description": "ACL PDP 决策引擎（四值决策 + 合并算法）",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@acl/policy-dsl": "workspace:*",
    "@acl/shared-types": "workspace:*"
  }
}
