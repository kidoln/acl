{
  "name": "@acl/policy-dsl",
  "version": "0.1.0",
  "private": true,
  "description": "ACL 策略选择器 DSL 解析器（Spike）",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
