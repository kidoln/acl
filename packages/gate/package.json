{
  "name": "@acl/gate",
  "version": "0.1.0",
  "private": true,
  "description": "ACL 发布门禁执行器（P0/P1/P2/P3）",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@acl/shared-types": "workspace:*",
    "@acl/validator": "workspace:*"
  }
}
