{
  "name": "@acl/constraints",
  "version": "0.1.0",
  "private": true,
  "description": "ACL 约束引擎（SoD/Cardinality）",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@acl/shared-types": "workspace:*"
  }
}
