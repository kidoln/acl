{
  "name": "@acl/api",
  "version": "0.1.0",
  "private": true,
  "description": "ACL 控制面与判权 API（阶段 1 骨架）",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/main.ts",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@acl/constraints": "workspace:*",
    "@acl/gate": "workspace:*",
    "@acl/engine": "workspace:*",
    "@acl/lifecycle": "workspace:*",
    "@acl/persistence": "workspace:*",
    "@acl/policy-dsl": "workspace:*",
    "@acl/shared-types": "workspace:*",
    "@acl/validator": "workspace:*",
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
